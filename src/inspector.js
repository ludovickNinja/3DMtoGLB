export function initInspector(app, viewer) {
    // Inspector initialization and event wiring
    // The main logic is in app.updateInspector()
    // This module serves as a placeholder for inspector-specific functionality

    // Initially show empty state
    document.getElementById('inspectorContent').innerHTML = '<div class="inspector-empty">No object selected</div>';

    // No additional setup needed - main.js handles all inspector logic
}
